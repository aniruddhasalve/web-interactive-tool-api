# Required for /v1/agent/tasks
OPENAI_API_KEY=replace-me

# Optional: defaults to the configured OpenAI-compatible endpoint in the runtime
OPENAI_API_BASE=
OPENAI_MODEL=gpt-5-mini
CHROMIUM_EXECUTABLE_PATH=
